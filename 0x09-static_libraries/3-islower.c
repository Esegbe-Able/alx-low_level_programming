#include "main.h"
/**
 * _islower - thins function checks for lowercase character
 * @c: char to be check
 * Return: 1 if c is lowercase and  0 if it is uppercase
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
