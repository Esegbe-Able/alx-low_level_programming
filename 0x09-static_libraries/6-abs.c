#include "main.h"


/**
 * _abs - computes the absolute value of an integer
 * @m: integer value to be check
 * Return: the absolute value of int
 */
int _abs(int m)
{
	if (m >= 0)
	{
		return (m);
	}
	return (-m);
}
