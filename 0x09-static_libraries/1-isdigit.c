#include "main.h"
		
/**
 * _isdigit - checks for a digit between  0 to 9
 * @c: integer value or digit to be checked
 * Return: 1 if c is a digit and 0 if is not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
